# MongoDB connection string (use MongoDB Atlas free tier, or local mongodb://localhost:27017/trackdb)
MONGODB_URI=
# Get this from https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Server port
PORT=5000

# Number of recent readings used to compute the trend slope
TREND_WINDOW=10
